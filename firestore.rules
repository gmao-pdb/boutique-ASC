rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Accès réservé aux utilisateurs connectés (équipe du club).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
